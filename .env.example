# Copy to .env. Only the API key is required — everything else has a sane
# default baked into the code. Uncomment a line below only to override it.
# See the README for what each variable does.

OPENROUTER_API_KEY=

# PORT=3000
# MOCK=true
# MOCK_TEXT=true
# MOCK_SPEECH=true
# MOCK_TEXT_DELAY_MS=900
# MOCK_SPEECH_DELAY_MS=900
# TTS_MODE=gemini
# LOG_LEVEL=debug
# RECORD_BROADCAST=true
